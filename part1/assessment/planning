specs for everything

functions
	write 'identical' functions, one w/ one w/o return

html and dom - give only text 
	generate a table of chosen size
	populate a table
	clear a table

dom app that prints a name - give first function
	refactor from one massive function
	to mvc functions

objects - give specs to fill out
	create an object with a name, and a method to print it
	have it modify that property
	give it a method that adds its args as a property

models - give empty object, methods too?
	series of console logs
	- add object
	- access object by id
	- modify object
	- get all

callbacks
	write the backend of a callback
	write the frontend of a callback
	convert sync to async



